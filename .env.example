# ──────────────────────────────────────────────────────────────────
# Core Web Ops — Root .env.example
#
# This file documents all environment variables used across the stack.
# Copy the relevant sections into backend/.env and frontend/.env.
# NEVER commit .env files with real secrets.
# ──────────────────────────────────────────────────────────────────

# ── See backend/.env.example for the full backend variable list ───
# DATABASE_URL=postgresql+psycopg2://USER:PASSWORD@HOST:5432/DB?sslmode=require
# SECRET_KEY=replace-with-a-long-random-string-min-32-chars
# ALGORITHM=HS256
# ACCESS_TOKEN_EXPIRE_MINUTES=30
# PORT=8000
# CORS_ORIGINS=http://localhost:5173
# EMAIL_PROVIDER=mock
# SMS_PROVIDER=mock

# ── See frontend/.env.example for the full frontend variable list ─
# VITE_API_URL=http://localhost:8000
